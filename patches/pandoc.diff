--- old/src/Text/Pandoc/Writers/LaTeX.hs
+++ new/src/Text/Pandoc/Writers/LaTeX.hs
@@ -268,13 +268,14 @@ stringToLaTeX  _     []     = return ""
 stringToLaTeX  ctx (x:xs) = do
   opts <- gets stOptions
   rest <- stringToLaTeX ctx xs
+  xelatex <- (elem "xelatex" . writerLaTeXArgs . stOptions) <$> get
   let ligatures = writerTeXLigatures opts && ctx == TextString
   let isUrl = ctx == URLString
   when (x == '€') $
      modify $ \st -> st{ stUsesEuro = True }
   return $
     case x of
-       '€' -> "\\euro{}" ++ rest
+       '€' | not xelatex -> "\\euro{}" ++ rest
        '{' -> "\\{" ++ rest
        '}' -> "\\}" ++ rest
        '`' | ctx == CodeString -> "\\textasciigrave{}" ++ rest
--- old/pandoc.cabal	2017-10-11 18:38:39.869046908 -0700
+++ new/pandoc.cabal	2017-10-11 20:44:32.373863548 -0700
@@ -436,7 +436,7 @@
                  bytestring >= 0.9 && < 0.11,
                  extensible-exceptions >= 0.1 && < 0.2,
                  skylighting >= 0.1.1.5 && < 0.2,
-                 aeson >= 0.7.0.5 && < 1.2,
+                 aeson >= 0.7.0.5 && < 1.3,
                  yaml >= 0.8.8.2 && < 0.9,
                  containers >= 0.1 && < 0.6,
                  skylighting >= 0.1.1.4 && < 0.2,
