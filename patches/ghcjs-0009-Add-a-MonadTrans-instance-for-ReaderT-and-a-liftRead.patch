diff --git a/src/Gen2/Object.hs b/src/Gen2/Object.hs
index bfa2fcc..649f45f 100644
--- a/src/Gen2/Object.hs
+++ b/src/Gen2/Object.hs
@@ -525,6 +525,12 @@ putListOf p xs = do
   lift (DB.putWord32le (fromIntegral $ length xs))
   mapM_ p xs
 
+instance MonadTrans (ReaderT r) where
+    lift   = liftReaderT
+
+liftReaderT :: m a -> ReaderT r m a
+liftReaderT m = ReaderT (const m)
+
 getListOf :: GetS a -> GetS [a]
 getListOf g = do
   l <- lift DB.getWord32le
